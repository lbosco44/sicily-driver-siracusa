'use client';

// Snap-to-section behavior was overriding user scroll agency.
// Replaced with no-op per design audit (Jakub Krehel motion principles).
export default function HeroSnap() {
  return null;
}
